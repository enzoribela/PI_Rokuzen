const {
  ROLES
} = require("../constants/validation.constants")

const PERMISSION_CONSTANTS = {
  AUTH: {
    CADASTRAR: [ROLES.GERENTE, ROLES.MASTER]
  },

  USUARIO: {
    LER: [ROLES.MASTER],
    ATUALIZAR: [ROLES.MASTER],
    DELETAR: [ROLES.MASTER]
  },

  SALA: {
    CRIAR: [ROLES.GERENTE, ROLES.MASTER],
    ATUALIZAR: [ROLES.GERENTE, ROLES.MASTER],
    DELETAR: [ROLES.GERENTE, ROLES.MASTER]
  },

  AGENDAMENTO: {
    LER: [ROLES.RECEPCIONISTA, ROLES.GERENTE, ROLES.MASTER],
    ATUALIZAR: [ROLES.GERENTE, ROLES.MASTER],
    DELETAR: [ROLES.GERENTE, ROLES.MASTER]
  }
}

module.exports = Object.freeze(PERMISSION_CONSTANTS)
